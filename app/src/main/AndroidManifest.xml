<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="AplicacionesMoviles_Ejercicios"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AplicacionesMoviles_Ejercicios"
        tools:targetApi="31">

        <activity android:name=".RegistroAsistenciaActivity" android:exported="false" />
        <activity android:name=".RegistroMatriculaActivity" android:exported="false" />
        <activity android:name=".CalculadoraActivity" android:exported="false" />
        <activity android:name=".MetodoBurbujaActivity" android:exported="false" />
        <activity android:name=".MainMenuActivity" android:exported="false" />

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>

</manifest>